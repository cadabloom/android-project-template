package com.cadabloom.trident.core;

import org.junit.Test;

import java.io.IOException;

public class UserRepositoryIntegrationTests {

    @Test
    public void user_service_login_integration_test() throws IOException, InterruptedException {
//        UserRepository repository = new UserRepository(TestServiceGenerator.createRXJavaClient(TestConstant.USER_API_BASE_URL));
//        TestObserver<String> to = new TestObserver<String>();
//        LoginUser request = new LoginUser("test@test.com", "password");
//        repository.login(request).subscribe(token -> {
//            Assert.assertNotNull(token);
//            //Assert.assertEquals(komixid, draft.getId());
//            latch.countDown();
//        });
//
//        latch.await();
    }
}
